# CommonGUI_byPython
GUI created by Python to run the another python script.

# build with command 
pyinstaller -F --onefile --windowed GUI.py

# Recommanded version
Pyinstaller == 3.6
Python == 3.9.13
